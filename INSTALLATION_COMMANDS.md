# 🎯 REAL WORKING SYSTEM - INSTALLATION COMMANDS

## 📥 CLONE THE REPOSITORY

```bash
# Clone the repository
git clone https://github.com/huskyin9-cyber/Amsfe.git

# Navigate to the directory
cd Amsfe
```

## 🔧 INSTALL DEPENDENCIES

```bash
# Update system packages
sudo apt-get update

# Install system dependencies
sudo apt-get install -y nmap git build-essential golang-go python3 python3-pip

# Install Python dependencies
pip3 install -r requirements.txt
```

## 🧪 VERIFY SYSTEM WORKS

```bash
# Run system verification (30 seconds)
python3 verify_system.py
```

## 🚀 RUN THE SYSTEM

### Option 1: Interactive Mode (Recommended)
```bash
# Start the interactive penetration system
python3 REAL_WORKING_SYSTEM.py
```

### Option 2: Component Test (Quick Test)
```bash
# Test all components (2-5 minutes)
python3 test_system.py
```

## 📋 COMPLETE INSTALLATION SEQUENCE

Copy and paste these commands in your terminal:

```bash
# 1. Clone repository
git clone https://github.com/huskyin9-cyber/Amsfe.git
cd Amsfe

# 2. Install dependencies
sudo apt-get update && sudo apt-get install -y nmap git build-essential golang-go python3 python3-pip
pip3 install -r requirements.txt

# 3. Verify system
python3 verify_system.py

# 4. Run the system
python3 REAL_WORKING_SYSTEM.py
```

## ⚠️ IMPORTANT NOTES

- **Total setup time**: 5-10 minutes
- **Full operation time**: 45-70 minutes
- **Authorization required**: Only use on systems you own or have written permission to test
- **System requirements**: 4GB+ RAM, internet connection

## 🎯 WHAT HAPPENS WHEN YOU RUN

When you run `python3 REAL_WORKING_SYSTEM.py`:

1. **Interactive menu appears**
2. **Enter target URL** (e.g., example.com)
3. **Confirm authorization** (type "yes")
4. **System runs for 45-70 minutes** performing:
   - Real proxy scraping (10-15 min)
   - Framework installation (15-30 min)
   - Reconnaissance (15-25 min)
   - Data extraction (20-30 min)
5. **Results saved** in `~/real_penetration_system/operation_[timestamp]/`

## 🔍 VERIFICATION OUTPUT

When you run `python3 verify_system.py`, you should see:

```
🎯 REAL WORKING SYSTEM - FINAL VERIFICATION
════════════════════════════════════════════════════════════
✅ PASS Dependencies
✅ PASS Python Modules
✅ PASS System Components
✅ PASS Proxy Functionality
✅ PASS AI Components

📊 VERIFICATION RESULTS: 5/5 checks passed
🚀 SYSTEM STATUS: FULLY OPERATIONAL
```

## 🎯 READY TO USE

After running these commands, you'll have a **REAL WORKING PENETRATION TESTING SYSTEM** ready for authorized security testing.

**Remember: Only use on systems you own or have written authorization to test!**